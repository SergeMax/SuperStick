package models;

import javafx.animation.PauseTransition;
import javafx.geometry.Bounds;
import javafx.util.Duration;

public class Party {
    private Bounds tirFBound;


    public Party() {

    }


    public void actuPosition(){


       // tirFBound = tirYeux.getBoundsInParent();

       // delayExploTouch = new PauseTransition(Duration.seconds(0.8));
        //delayExploTouch.setOnFinished(event -> {

      //      if (tirYeux.getBoundsInParent().intersects(drone1.getBoundsInParent())) {

       //     }
}


}
